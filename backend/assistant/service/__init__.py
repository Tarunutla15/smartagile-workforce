"""
Assistant HTTP layer: chat session + message API views and their URLs.

The reply orchestration entrypoint lives in `assistant.brain`; the LangGraph workflow
itself lives in `assistant.graph`; semantic memory lives in `assistant.memory`.
"""

